// Perfil del empleado activo: nombre, foto, servicios propios
// (EmpleadoServicio), vacaciones (Cierre alcance=EMPLEADO), horarios
// (Horario). Los datos generales de la barbería NO se editan acá
// (eso es /configuracion/barberia, solo para OWNER).
// TODO: formularios de edición por sección.

export default function PerfilPage() {
  return <main>{/* TODO: edición de perfil del empleado */}</main>;
}
