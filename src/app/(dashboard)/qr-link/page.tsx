// Muestra el QR y el link público de la barbería (slugPublico), listos
// para compartir. Llevan directo a /pedir-turno/[slug] o /resena/[slug].
// TODO: generar imagen QR a partir del link (on-demand, no se guarda en BD).

export default function QrYLinkPage() {
  return <main>{/* TODO: QR + link público de la barbería */}</main>;
}
