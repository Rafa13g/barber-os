// Cantidad de clientes, ganancias, etc. En vivo (agregado sobre Turno),
// con opción de descargar en Excel (crea un ReporteDescargable).
// Empleado ve el propio; solo OWNER ve el de toda la barbería.
// TODO: agregaciones + botón de descarga -> /api/reportes.

export default function ReportesPage() {
  return <main>{/* TODO: dashboard de reportes */}</main>;
}
