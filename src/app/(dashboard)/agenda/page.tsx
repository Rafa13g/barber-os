// Vista de agenda del empleado activo: día / semana / mes.
// Filtros: horario de llegada, mayor/menor valor, tipo de servicio.
// Botón "+" -> /agenda/nuevo-turno (carga manual).
// TODO: componente de calendario + fetch de Turno por empleadoId.

export default function AgendaPage() {
  return <main>{/* TODO: calendario día/semana/mes + filtros + botón "+" */}</main>;
}
