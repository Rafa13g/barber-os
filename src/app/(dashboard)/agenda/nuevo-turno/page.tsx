// Carga manual de un turno (botón "+" de la agenda), para clientes
// que no quieren usar la app. creadoManualmente=true,
// creadoPorEmpleadoId = empleado logueado en la sesión.
// TODO: formulario (cliente, servicio, empleado asignado, horario).

export default function NuevoTurnoManualPage() {
  return <main>{/* TODO: formulario de turno manual */}</main>;
}
