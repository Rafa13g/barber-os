// Alta/baja (soft delete) de empleados, asignación de rol. Solo OWNER.
// TODO: listado + formulario de alta + soft delete.

export default function ConfiguracionEmpleadosPage() {
  return <main>{/* TODO: gestión de empleados (solo OWNER) */}</main>;
}
