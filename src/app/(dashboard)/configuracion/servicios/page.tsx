// Catálogo de servicios de la barbería (precio base, duración base).
// El precio/duración por barbero se ajusta en el perfil de cada uno
// (EmpleadoServicio), no acá.
// TODO: CRUD de Servicio.

export default function ConfiguracionServiciosPage() {
  return <main>{/* TODO: catálogo de servicios */}</main>;
}
