// Datos generales de la barbería (ubicación, teléfono, redes, logo,
// portada, estado). Solo editable por rol OWNER.
// TODO: guard de rol + formulario.

export default function ConfiguracionBarberiaPage() {
  return <main>{/* TODO: edición de datos de la barbería (solo OWNER) */}</main>;
}
