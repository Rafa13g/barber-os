// Login de la cuenta de la barbería (email + password).
// Recordar: auth opción A -> una sola cuenta por barbería. Después de
// loguearse, redirige a /seleccionar-empleado.
// TODO: formulario + integración NextAuth (paso futuro).

export default function LoginPage() {
  return <main>{/* TODO: formulario de login */}</main>;
}
