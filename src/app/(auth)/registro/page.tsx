// Alta de una nueva barbería: crea Barberia + Usuario (owner) +
// Empleado (rol OWNER) en una misma transacción.
// Pide: nombre, ubicación, teléfono, redes, empleados iniciales,
// servicios, horarios, estado.
// TODO: formulario multi-paso + POST a /api/barberias.

export default function RegistroPage() {
  return <main>{/* TODO: formulario de registro de barbería */}</main>;
}
