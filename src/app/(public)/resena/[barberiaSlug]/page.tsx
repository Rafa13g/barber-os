// Dejar una reseña y ver reseñas anteriores de la barbería.
// TODO: listado de Resena + formulario para crear una nueva.

export default function ResenasPage({
  params,
}: {
  params: { barberiaSlug: string };
}) {
  return <main>{/* TODO: reseñas de {params.barberiaSlug} */}</main>;
}
