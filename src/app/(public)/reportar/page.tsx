// Formulario de error/sugerencia. Envía a netloom.uy@gmail.com vía
// /api/contacto. Accesible desde el botón global en el layout raíz.
// TODO: formulario (asunto, mensaje, email opcional de contacto).

export default function ReportarPage() {
  return <main>{/* TODO: formulario de reporte */}</main>;
}
